[plugin]
id = "com.revdeck.examples.valid-minimal"
name = "Valid Minimal Plugin"
version = "0.1.0"
sdk_version = "0.1.0"
revdeck_compat = ">=0.1,<0.3"

[[capabilities]]
id = "minimal-adapter"
kind = "adapter"
inputs = ["json"]
outputs = ["object_batch"]

[[capabilities]]
id = "minimal-report-contributor"
kind = "report_contributor"
inputs = ["object_batch"]
outputs = ["finding_context"]

[permissions]
artifact_read = ["external_export"]
project_write = ["objects", "edges", "attributes", "diagnostics"]
lab_write = ["plugin"]
network = false
process_spawn = false
secrets = false
environment = false

[ui]
commands = ["minimal.import"]
inspector_cards = ["minimal.provenance"]
